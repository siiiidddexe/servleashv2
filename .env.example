# SurrealDB credentials (used by both surrealdb service and backend)
SURREALDB_USER=root
SURREALDB_PASS=root

# Mail API
MAIL_API_URL=https://nexomail.logiclaunch.in
MAIL_API_KEY=your-mail-api-key

# Host port for the frontend container (default 80)
PORT=80
